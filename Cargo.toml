[workspace]

members = [ 
    "loony-algo",
    "loony-tokio",
    "loony-redis"
]
resolver = "2"

exclude = [
    "loony-tokio"  # Exclude the package from default build
]
# cargo build --release -p loony-tokio
